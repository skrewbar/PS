#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve() {
    int N, M;
    ll K;
    cin >> N >> M >> K;

    cout << M << '\n';
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int Q;
    cin >> Q;

    while (Q--) solve();

    return 0;
}